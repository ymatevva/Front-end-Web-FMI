# 🧩 Задача 1: Анализ и модификация на текст

## Условие:
Създайте функция `analyzeText(text, options)`, която анализира и модифицира подаден текст според зададените опции.

### Параметри:
- `text` — входен текст за анализ (ако не е string, връща празен обект `{}`)
- `options` — обект с настройки (по подразбиране празен обект)

### Опции:
- **`trim`** — премахва водещи и завършващи интервали (по подразбиране: `true`)  
- **`toLowerCase`** — преобразува текста до малки букви (по подразбиране: `false`)  
- **`charCount`** — ако е `true`, добавя свойство `charCount` към резултата (без интервали)  
- **`mostCommonWord`** — ако е `true`, намира най-често срещаната дума в текста (без значение от case sensitive)

### Резултат:
Функцията връща **обект** със следната структура:
```js
{
  formattedText: "...",
  charCount: ...,
  mostCommonWord: "..."
}
```

### Тестови случаи:
```js
  console.log(analyzeText("A quick brown fox jumps over the lazy dog", { charCount: true }));
  // { formattedText: "A quick brown fox jumps over the lazy dog", charCount: 33 }

  console.log(analyzeText("test test TEST best", { toLowerCase: true, mostCommonWord: true }));
  // { formattedText: "test test test best", mostCommonWord: "test" }

  console.log(analyzeText(42, {}));
  // {}

  console.log(analyzeText("  Hello world!  ", { trim: false }));
  // { formattedText: "  Hello world!  " }
```

### ✨ TIP:
За да премахнете повтарящи се интервали между думите, можете да използвате следния регулярен израз:
```js
const singleSpacedText = text.replace(/\s+/g, ' ');
```
Този израз намира едно или повече (`+`) whitespace разстояния (`\s`) в целия низ (`g`) и ги заменя с единствен интервал.
